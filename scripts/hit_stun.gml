/// hit_stun(step)
// freezes (well, actually it slows) the game for a small period of time
// step: number of steps to freeze the game for

obj_hit_stun.stunned = true
obj_hit_stun.steps = argument0
